<Window x:Class="UPMurtazinIS121.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UPMurtazinIS121"
        mc:Ignorable="d"
        Title="Ингредиенты" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <!-- Список ингредиентов -->
        <ListBox Grid.Column="0" 
                 ItemsSource="{Binding IngredientsList}"
                 SelectedItem="{Binding SelectedIngredient}"
                 DisplayMemberPath="IngredientsName"/>

        <!-- Детали выбранного ингредиента -->
        <StackPanel Grid.Column="1" Margin="10" DataContext="{Binding SelectedIngredient}">
            <TextBlock Text="Детали ингредиента" FontWeight="Bold" Margin="0,0,0,10"/>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <!-- Добавьте строки для других свойств -->
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Название:" Grid.Row="0" Grid.Column="0"/>
                <TextBox Text="{Binding IngredientsName}" Grid.Row="0" Grid.Column="1" Margin="5"/>

                <TextBlock Text="Тип:" Grid.Row="1" Grid.Column="0"/>
                <TextBox Text="{Binding TypeIngredients}" Grid.Row="1" Grid.Column="1" Margin="5"/>

                <!-- Добавьте другие свойства по аналогии -->
            </Grid>
        </StackPanel>
    </Grid>
</Window>