<Window x:Class="UPMurtazinIS121.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UPMurtazinIS121"
        mc:Ignorable="d"
        Title="Учет ингредиентов - Кофейный Рай" 
        Height="790" Width="1200"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <TextBlock Text="УЧЕТ ИНГРЕДИЕНТОВ" 
                   FontSize="24" FontWeight="Bold" 
                   Foreground="{StaticResource AccentBrush}"
                   HorizontalAlignment="Center" 
                   VerticalAlignment="Top"
                   Margin="0,10"/>

        <Grid Margin="0,50,0,50">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="1.2*"/>
            </Grid.ColumnDefinitions>

            <!-- Левая часть - Список ингредиентов -->
            <Border Grid.Column="0"
                    Background="{StaticResource SecondaryBackgroundBrush}"
                    CornerRadius="5"
                    Padding="10">
                <ListView ItemsSource="{Binding IngredientsList}"
                          SelectedItem="{Binding SelectedIngredient}"
                          BorderThickness="0"
                          ScrollViewer.CanContentScroll="False"
                          ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical" Margin="5">
                                <TextBlock Text="{Binding IngredientsName}" 
                                           FontWeight="Bold" 
                                           FontSize="16"/>
                                
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding TypeIngredients}" FontStyle="Italic"/>
                                    <TextBlock Text="Минимальное количество: " FontStyle="Italic"
                                               Margin="50,0,0,0" />
                                    <TextBlock Text="{Binding MinimKolich}" FontStyle="Italic"/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal">
                                    <TextBlock>
                                        <TextBlock.Text>
                                            <MultiBinding StringFormat="{}{0:N2} {1}">
                                                <Binding Path="KolichSklad"/>
                                                <Binding Path="EdinIzmereniya"/>
                                            </MultiBinding>
                                        </TextBlock.Text>
                                    </TextBlock>

                                    <TextBlock Text="Количество в упаковке: " FontStyle="Italic"
                                               Margin="50,0,0,0" />
                                    <TextBlock Text="{Binding KolichUpakovka}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Цена: " FontStyle="Italic"
                                               Margin="50,0,0,0" />
                                    <TextBlock Text="{Binding CostForOne}"/>
                                    <TextBlock Text="Стоимость мин. партии: " FontStyle="Italic"
                                               Margin="50,0,0,0" />
                                    <TextBlock Text="{Binding MinOrderCost}"/>
                                </StackPanel>

                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Border>

            <GridSplitter Grid.Column="1" Width="6" Background="{StaticResource AccentBrush}" IsEnabled="False"/>

            <!-- Правая часть - Редактирование -->
            <Border Grid.Column="2"
                    Background="{StaticResource SecondaryBackgroundBrush}"
                    CornerRadius="5"
                    Padding="15">
                <StackPanel>
                    <TextBlock Text="Редактирование ингредиента" 
                               FontSize="18" FontWeight="Bold"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,15"/>

                    <Label Content="Название:"/>
                    <TextBox Text="{Binding SelectedIngredient.IngredientsName, UpdateSourceTrigger=PropertyChanged}"/>

                    <Label Content="Тип:"/>
                    <TextBox Text="{Binding SelectedIngredient.TypeIngredients, UpdateSourceTrigger=PropertyChanged}"/>

                    <Label Content="Ед. измерения:"/>
                    <TextBox Text="{Binding SelectedIngredient.EdinIzmereniya, UpdateSourceTrigger=PropertyChanged}"/>

                    <Label Content="На складе:"/>
                    <TextBox Text="{Binding SelectedIngredient.KolichSklad, StringFormat={}{0:N2}, UpdateSourceTrigger=PropertyChanged}"/>

                    <Label Content="Мин. кол-во:"/>
                    <TextBox Text="{Binding SelectedIngredient.MinimKolich, UpdateSourceTrigger=PropertyChanged}"/>

                    <Label Content="Цена за ед.:"/>
                    <TextBox Text="{Binding SelectedIngredient.CostForOne, StringFormat={}{0:N2}, UpdateSourceTrigger=PropertyChanged}"/>

                    <Label Content="Стоимость мин. партии:" FontWeight="Bold"/>
                    <TextBlock Text="{Binding SelectedIngredient.MinOrderCost, StringFormat={}{0:N2} руб.}" 
                               FontSize="16" FontWeight="Bold" Margin="0,0,0,20"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="СОХРАНИТЬ ВСЕ" 
                                Command="{Binding SaveCommand}"
                                Background="#5D4037"
                                Foreground="White"
                                FontWeight="Bold"
                                Margin="0,0,10,0"
                                Padding="20,10"
                                MinWidth="180"/>

                        <Button Content="+ ДОБАВИТЬ НОВЫЙ"
                                Command="{Binding AddNewCommand}"
                                Background="#689F38"
                                Foreground="White"
                                FontWeight="Bold"
                                Padding="20,10"
                                MinWidth="180"/>
                        
                        <Button Content="Удалить" Command="{Binding DeleteCommand}" 
                                Background="#D32F2F"
                                Foreground="White"
                                FontWeight="Bold"
                                Margin="10,0,0,0"
                                Padding="20,10"
                                MinWidth="180"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <StatusBar VerticalAlignment="Bottom" Background="{StaticResource AccentBrush}">
            <StatusBarItem>
                <TextBlock Text="Кофейный Рай © 2023" Foreground="{StaticResource LightTextBrush}"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Всего ингредиентов: " Foreground="{StaticResource LightTextBrush}"/>
                    <TextBlock Text="{Binding IngredientsList.Count}" Foreground="{StaticResource LightTextBrush}"/>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>